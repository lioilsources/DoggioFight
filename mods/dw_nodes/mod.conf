name = dw_nodes
title = DoggioWars nodes
description = Stand-ins for the minetest_game nodes DoggioWars builds terrain from (default:*, flowers:*, fire:basic_flame), so the game ships without bundling minetest_game.
min_minetest_version = 5.12
